<!--Pages/wxs/wxs.wxml-->
<!--错误的写法  保留两位小树-->
<!-- <view>{{25.645654987654.toFixed(2)}}</view> -->

<!--2.wxs的定义方式-->  
<!--2.1直接在wxml中定义-->
<!-- <wxs module="info"> 

  
    var message = "Hello Workd";
    var name = "codeWhy";

    var sum = function(num1, num2) {
      return num1 + num2
    }

    //commonjs的模块化导出的写法
    module.exports = {
        message: message,
        name: name,
        sum: sum
    }
</wxs> -->

<!--2.2 定义在单独的wxs文件中，在使用<wxs>标签进行导入-->
<!--不能使用绝对路径， 必须使用相对路径-->
<wxs src="../../wxs/info.wxs" module="info"></wxs>

<view>{{info.message}}</view>
<view>{{info.name}}</view>
<view>{{info.sum(20, 30)}}</view>

<!--3.wxs的应用-->
<wxs src="../../wxs/format.wxs" module="format"></wxs>
<view>{{price}}</view>

<view>{{format.priceFormat(price, 3)}}</view>